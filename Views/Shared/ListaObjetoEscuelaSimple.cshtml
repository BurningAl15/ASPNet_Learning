@* @model List<ObjetoEscuelaBase> *@
    
<table class="table table-dark table-hover table-sm">
    <thead>
        <tr>
            <th scope="col">
                Nombre
            </th>
            <th scope="col">
                Id
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model){
        <tr>
            <td>@obj.Name</td>
            <td>@obj.UniqueId</td>
        </tr>
        }
        @* @for(int i=0;i<Model.Count;i++){
            <tr>
                <td>@(i+1)) @Model[i].Name</td>
                <td>@Model[i].UniqueId</td>
            </tr>
        } *@
    </tbody>
</table>

